#ifndef COMMON_H
#define COMMON_H

// Must include Windows before any C++ standard headers
// to avoid std::byte conflict
#include <windows.h>

// Now include C++ standard library headers
#include <string>
#include <iostream>
#include <fstream>
#include <sstream>

using namespace std;

#endif
